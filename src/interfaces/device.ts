const size = {
    small: '768px',
    medium: '992px',
    large: '1200px'
}

export const device = {
    small: `(min-width: ${size.small})`,
    medium: `(min-width: ${size.medium})`,
    large: `(min-width: ${size.large})`
}

